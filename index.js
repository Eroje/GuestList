var guestName = prompt("What is your name?");

var guestList = ["Angela", "Tonia","Judith", "Quincy", "Mojid"];

if (guestList.includes(guestName)) {
     alert ("Welcome!");
  
  } else {
    alert ("Maybe next time!");
}
